{
  "id": "character-creation",
  "version": "1.1",
  "name": "Create a character",
  "description": "Draft character image variants from a description. The agent scores and selects the strongest reference; all variants remain in the graph.",
  "inputs": [
    { "key": "description", "type": "text", "required": true, "label": "Character description" }
  ],
  "steps": [
    {
      "id": "draft",
      "kind": "capability",
      "capability": "image.generate",
      "inputs": [{ "from": "input:description", "role": "prompt" }],
      "promptRef": "prompts/character.md",
      "values": {},
      "variants": {
        "count": 5,
        "vary": {
          "style": ["anime", "photoreal", "ink", "3d", "watercolor"]
        }
      },
      "select": { "mode": "one", "by": "agent" }
    }
  ],
  "output": { "type": "image", "from": "step:draft" }
}
